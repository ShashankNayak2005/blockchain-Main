{
  "name": "secure-file-sharing-blockchain",
  "version": "1.0.0",
  "description": "Cloud-Based Secure File Sharing Using Blockchain and AES-256 Encryption",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix blockchain install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "blockchain:node": "npm --prefix blockchain run node",
    "blockchain:deploy": "npm --prefix blockchain run deploy",
    "test:backend": "npm --prefix backend test",
    "test:blockchain": "npm --prefix blockchain test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
